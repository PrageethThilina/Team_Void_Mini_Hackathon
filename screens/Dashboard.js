import React from 'react'
import { View, Text } from 'react-native'

const Chat = ({ route, navigation }) => {
  return (
    <View>
      <Text>Dashboard</Text>
    </View>
  )
}

export default Chat
